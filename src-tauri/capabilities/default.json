{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MySkills main window capability set. Filesystem, SQL, shell, and HTTP are intentionally not exposed to the renderer.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
